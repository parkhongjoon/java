package sec06.exam02;

/* 다른 패키지에 C클래스를 만든 후 B와 같이 a를 만들어 호출해 보자
 * C 클래스가 있는 패키지에 D클래스를 만든 후 A 클래스를 상속하여
 * 호출해 보고 객체를 만들어 메소드 필드를 호출해보자 */
//import sec06.exam01.A;

public class C {
	//메소드 선언
	public void method() {
//		A a = new A(); // ---- 같은 패키지 안에서 protected는 사용 가능
//		a.field = "value";
//		a.method();
	}
}
