package sec06.exam01;

/* class A 를 public 으로 생성한다.
 * 안에는 string 타입의 field와 method()가 있다.
   이 필드와 메소드는 내부 또는 자식클래스만이 접근 가능하다.*/
public class A {
	//필드
	protected String field;
	
	//메소드
	protected void method() {};
}
